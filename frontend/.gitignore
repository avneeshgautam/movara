.dart_tool/
.flutter-plugin-dependencies
.flutter-plugins-dependencies
.packages
build/
*.iml
.idea/
.DS_Store

# Cloudflare Wrangler cache — holds account id/email, not for git.
.wrangler/

# Build artifacts produced by `flutter build web` + zip.
movara-web.zip

# Platform folders (android/, ios/, web/, etc.) are generated by
# `flutter create .` — see frontend/README.md — and should be committed
# once they exist, so they're intentionally not ignored here.


# Firebase client config — kept out of git so secret scanners stay quiet.
firebase.env
